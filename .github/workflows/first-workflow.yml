name: KaamWala CI

on:
  push:
    branches:
      - main

jobs:
  first-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Print Working Directory
        run: pwd

      - name: List Files
        run: ls -la

      - name: Say Hello
        run: echo "Hello Bandariya!"
